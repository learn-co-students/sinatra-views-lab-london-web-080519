<!DOCTYPE html>
<html>
    <head>
        <title>Today</title>
    </head>

    <body>
        <h1>Today</h1>
        <p><%= "The date is "%> <%=Date.today.strftime("%A, %B %d, %Y") %> </p>
        # required 'date' in the environment file
    </body>

</html>